<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper

		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"

		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bside.BSIDE.user.persistence.UserMapper">
	<insert id="insertUser" parameterType="com.bside.BSIDE.user.domain.UserDto">
		INSERT INTO
			bside.user(
			eml,
			usr_nm,
			password,
			gndr_cls_cd,
			brdt,
			join_dtm
		)
		VALUES(
				  #{eml},
				  #{usrNm},
				  #{password},
				  #{gndrClsCd},
				  #{brdt},
				  now()
			  )
	</insert>
	
	<select id="getAllUsers" resultType="com.bside.BSIDE.user.domain.UserDto">
		SELECT * FROM bside.user
	</select>
	
	<update id="updateUser" parameterType="com.bside.BSIDE.user.domain.UserDto">
		UPDATE bside.user SET (
			name=#{name}, 
			usr_nm=#{usrNm}, 
			gndr_cls_cd=#{gndrClsCd}, 
			brdt=#{brdt},
			updateDtm=now() 
		)
			WHERE eml=#{userId}
	</update>
	
	<delete id="deleteUser" parameterType = "String">
		DELETE FROM bside.cust 
		WHERE eml = #{userId}
	</delete>
</mapper>